apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: virtual-service-broken-weight-text-auto
spec:
  hosts:
  - reviews
  http:
  - route:
    - destination:
        name: reviews
        subset: v1
      weight: 40a
    - destination:
        name: reviews
        subset: v2
      weight: 50
